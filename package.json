{
  "name": "vitest-browser-typescript",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "npm run test:node && npm run test:browser",
    "test:node": "vitest run",
    "test:browser": "vitest run -c vitest.browser.config.ts"
  },
  "dependencies": {
    "@vitest/browser": "^3.0.4",
    "playwright": "^1.50.1",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
